# Python dependencies for polyglot-ai
# These packages will be automatically installed during npm install

# Data processing
# Note: Requires numpy>=2.3.5 and pandas>=2.3.3 for Python 3.14 wheel support
numpy>=2.3.5
pandas>=2.3.3

# Excel file operations
openpyxl>=3.1.5  # Read/write Excel files (used by pandas)

# Web and HTTP
requests>=2.32.5

# Date and time
python-dateutil>=2.8.0

# JSON and data formats
pyyaml>=6.0.3

# Excel automation with COM (works with embeddable Python + pywin32)
xlwings>=0.33.16
pywin32>=311  # Required by xlwings for Windows COM interface

# Document conversion (PDF, Word, PowerPoint, Excel to Markdown)
markitdown[pdf,docx,pptx,xlsx]>=0.1.3

# Native mixed-DPI, multi-monitor screenshots on Windows
pillow>=12.0.0

# PDF to DOCX conversion
pdf2docx>=0.5.8

# Add more packages as needed
# matplotlib>=3.7.0
# beautifulsoup4>=4.12.0
